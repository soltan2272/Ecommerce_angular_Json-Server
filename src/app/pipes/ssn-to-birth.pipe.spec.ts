import { SsnToBirthPipe } from './ssn-to-birth.pipe';

describe('SsnToBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new SsnToBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
