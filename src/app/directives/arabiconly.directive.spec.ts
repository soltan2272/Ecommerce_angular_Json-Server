import { ArabiconlyDirective } from './arabiconly.directive';

describe('ArabiconlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ArabiconlyDirective();
    expect(directive).toBeTruthy();
  });
});
